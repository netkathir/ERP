-- Create Database for ERP System
-- Run this script in MySQL to create the database

CREATE DATABASE IF NOT EXISTS `basic_template` 
CHARACTER SET utf8mb4 
COLLATE utf8mb4_unicode_ci;

-- Grant privileges (adjust username as needed)
-- GRANT ALL PRIVILEGES ON basic_template.* TO 'root'@'localhost';
-- FLUSH PRIVILEGES;

-- Use the database
USE `basic_template`;

